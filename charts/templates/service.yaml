apiVersion: v1
kind: Service
metadata:
  name: {{include "guggr.fullname" .}}
  labels: {{- include "guggr.labels" . | nindent 4}}
spec:
  type: {{.Values.service.type}}
  ports:
    - port: {{.Values.service.port}}
      targetPort: http
      protocol: TCP
      name: http
  selector: {{- include "guggr.selectorLabels" . | nindent 4}}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "guggr.fullname" . }}-test-connection"
  labels:
    {{- include "guggr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "guggr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

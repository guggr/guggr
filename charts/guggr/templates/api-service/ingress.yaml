{{- if .Values.apiService.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "api-service.fullname" . }}
  labels:
    {{- include "api-service.labels" . | nindent 4 }}
  {{- with .Values.apiService.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.apiService.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.apiService.ingress.tls }}
  tls:
    {{- range .Values.apiService.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.apiService.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "api-service.fullname" $ }}
                port:
                  number: {{ $.Values.apiService.service.port }}
          {{- end }}
    {{- end }}
{{- end }}

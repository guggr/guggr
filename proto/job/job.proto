syntax = "proto3";
package guggr.job.v1;

import "job/types/http.proto";
import "job/types/ping.proto";

import "buf/validate/validate.proto";

message Job {
    string id = 1 [(buf.validate.field).required = true];

    JobType job_type = 16;
}

enum JobType {
    JOB_TYPE_UNSPECIFIED = 0;
    JOB_TYPE_HTTP = 1;
    JOB_TYPE_PING = 2;
}
